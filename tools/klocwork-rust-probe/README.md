# Klocwork Rust probe

This standalone crate contains intentionally defective Rust code used only to
verify Klocwork integration-build analysis. It is not a dependency of the
application, the existing Rust selector, or any test project.

Do not call or copy these functions into production code.

